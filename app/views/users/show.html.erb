 <div class="jumbotron">
   <p> There is nothing so fatal to character as half finished tasks. - David Lloyd George </p> 
 </div>
 
<div class="row">
   <div class="col-md-4">
     <h2>Completed Tasks</h2>
     <p>List of Tasks completed</p>
   </div>
   <div class="col-md-4">
     <h2>Tasks for the Week</h2>
     <% @items.each do |item| %>
       <ol> <%= item.name %> </ol>
     <% end %>
   </div>
   <% if current_user %>
     <div class="col-md-4">
       <h2>Add New Tasks for the Week</h2>
       <%= link_to "New Item", new_user_item_path(@user, @item), class: 'btn btn-success' %>
     </div>
   <% end %>
   
</div>
